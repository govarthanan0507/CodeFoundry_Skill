# Ideation

This is the directly testable Ideation agent package. The substantive V1 specification is preserved from the original `AGENTS/Ideation/AGENT.md`.


## Standalone packaging note

This agent includes a local copy of `STAGE_EXIT_CONTRACT.md` so the Ideation Agent can be distributed, tested, or run independently. The canonical CodeFoundry copy remains under `SHARED/`; the local copy is the same universal contract, not a separate variant.
