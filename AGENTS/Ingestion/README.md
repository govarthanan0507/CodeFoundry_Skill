# Ingestion

This folder is a directly testable CodeFoundry agent unit. Its
`UNIT.json` is the machine-readable test boundary.

Sits in front of `AGENTS/Ideation/`, not instead of it — the fast
entry point for when the thinking (or the code) already exists,
rather than making a fully-formed idea or an existing product repeat
Ideation's from-scratch conversational discovery.
